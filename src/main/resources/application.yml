spring:
  application:
    name: virtual-psychologist-backend

  main:
    banner-mode: off

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres